syntax = "proto3";

import "google/protobuf/empty.proto";

option go_package = "internal/pb/server;server";

service ServerService {
  rpc Ping(google.protobuf.Empty) returns (ServerResponse);
}
message ServerResponse {
  bool run = 1;
}