syntax = "proto3";

option go_package = "api/pb/auth;auth";

service AuthService {
  rpc Login(LoginRequest) returns (LoginResponse);
}

message LoginRequest {
  string username = 1;
  string password = 2;
}

message LoginResponse {
  uint64 id = 1;
  string token = 2;
  string username = 3;
}